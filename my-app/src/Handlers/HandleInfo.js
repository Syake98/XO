// export const handleInfo = (isDrow, isGameEnded, currentPlayer) => {
// 	if (isDrow) {
// 		return 'Ничья';
// 	} else if (!isDrow && isGameEnded) {
// 		return `Победа: ${currentPlayer}`;
// 	} else if (!isDrow && !isGameEnded) {
// 		return `Ходит: ${currentPlayer}`;
// 	}
// };
